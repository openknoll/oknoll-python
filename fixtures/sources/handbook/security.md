# Security policy

Access is deny by default; grants are explicit and scoped.

## Credentials

Credentials are stored hash-only. Static keys are forbidden.

## Reviews

Every dependency bump gets a security review.
