[project]
name = "oknoll-eval"
version = "0.3.5"
description = "OpenKnoll PD-vs-RAG evaluation harness: corpora, runner, grading, metrics, reports"
requires-python = ">=3.12"
license = "Apache-2.0"
readme = "README.md"
dependencies = [
    "okf-core==0.3.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/oknoll_eval"]
